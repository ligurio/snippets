os:
  - linux

sudo: required
dist: trusty

language: c

compiler:
  - clang
  - gcc

before_install:
  - if [[ "$TRAVIS_OS_NAME" == "linux" ]]; then sudo apt-get update -qq; fi

install:
  - sudo apt-get install tidy afl

script:
  - make
  - make check
  - make test
  - make -C tests test-fuzz
